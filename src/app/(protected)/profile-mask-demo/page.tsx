'use client';

import { ProfileMaskContainer } from '@/components/profile-mask';

export default function ProfileMaskDemoPage() {
  return (
    <div className="min-h-screen bg-[var(--bg)]">
      <ProfileMaskContainer />
    </div>
  );
}
