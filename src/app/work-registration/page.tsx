import { WorkRegistrationForm } from "@/components/work-registration";

export const metadata = {
  title: "作品登録 | VNS",
  description: "AIによる自動補完機能を備えた作品登録フォーム",
};

export default function WorkRegistrationPage() {
  return <WorkRegistrationForm />;
}
