# 2026-03-02 国際化（i18n）の基盤 TODO

> 方針: 将来のために最初から多言語対応を整備。Next.js 16の非同期制約・Proxyルールに完全に準拠する。
> 対象: next-intlを用いた言語切替 / Next.js 16 App Router Proxy / Async API / ヘッダー切替UI連携

---

## 🎯 ゴール

1. コード内から日本語直書きを排除
2. `src/proxy.ts` 内での安全な言語ルーティング検出（`middleware.ts`使用禁止）
3. App Routerの非同期パラメーター (`await params`) ルールを徹底しビルドエラーを回避
4. **[NEW] ヘッダー上の統合UIボタン**の実装と言語Cookieの保存・同期連携

---

## ✅ Priority 1: i18n フレームワーク導入と Next.js 16 準拠（3-4時間）

### 1. `next-intl` ライブラリ設定
- [ ] 1.1 `next-intl` インストール
- [ ] 1.2 `generate-i18n-types.ts` 等による型生成スクリプト導入

### 2. Next.js 16 Proxy ルーティングの構築
- [ ] 2.1 プロジェクトルートの `middleware.ts` ではなく、`src/proxy.ts` において Next-intl の言語ルーティングロジックを統合する
  ```typescript
  // src/proxy.ts
  import createMiddleware from 'next-intl/middleware';

  const intlProxy = createMiddleware({
    locales: ['ja', 'en'],
    defaultLocale: 'ja',
  });

  export async function proxy(request: NextRequest) {
    // intlProxy と既存の認証・ヘッダー処理などを統合
    return intlProxy(request);
  }
  ```
- [ ] 2.2 `next.config.ts` から Pages Router 固有の `i18n` 設定を排除（App Routerベースの `next-intl/plugin` 構成を利用）

---

## 🟠 Priority 2: コード内メッセージの翻訳化と非同期対応（4-5時間）

### 3. Params / SearchParams の非同期化対応
- [ ] 3.1 ページレイアウト（`[locale]/layout.tsx` や `[locale]/page.tsx`）が受け取る `params.locale` へのアクセスをすべて `const { locale } = await params;` へ変更する（Next.js 16必須ルール）
- [ ] 3.2 `setRequestLocale`等での非同期呼び出しチェック

### 4. エラーメッセージ・UIの国際化
- [ ] 4.1 エラーメッセージ辞書を翻訳キーへ変更
- [ ] 4.2 `useTranslations` でUIコンポーネント内を置換

---

## 🟡 Priority 3: ロケール依存フォーマットとヘッダー連携（2-3時間）

### 5. ヘッダーからの言語(i18n)切替機能の実装
- [ ] 5.1 `src/components/layout/header/` 内に言語切替UI（Dropdownなど）を実装
- [ ] 5.2 ボタンによる言語切り替え時、Cookie（NEXT_LOCALE 等）へ保存し、ログイン時はDBへの保存ロジックへの連携を構築
- [ ] 5.3 URLルーティング（`/ja/` ↔ `/en/`）のシームレスなトランジションの実装

### 6. 日付・時刻・通貨・数値フォーマット
- [ ] 6.1 `Intl.DateTimeFormat`, `Intl.NumberFormat` の活用
- [ ] 6.2 RTL対応準備（アラビア語などを想定した flex-row などのCSS見直し）

---

## 🔵 Priority 4: 翻訳管理フロー構築（2-3時間）

### 7. ガイド・チェックツール
- [ ] 7.1 翻訳ディレクトリ構成と翻訳作業ガイド
- [ ] 7.2 存在しない翻訳キーへのアクセスをビルド時に弾くTypeScript設定

---

## 📊 進捗
- 完了: 0 / 41
- ステータス: 🔄 未開始
- 最後の更新: 2026-03-02 レビューによる Next.js 16 Proxy ルールへの完全移行、非同期APIルール、ヘッダー言語トグルボタンの要件追加
