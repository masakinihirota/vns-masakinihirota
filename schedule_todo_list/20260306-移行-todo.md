## Plan: anti-supabaseランディングページの vns-masakinihirota への段階的移設（Option D採用）

この計画は **Option D: ハイブリッド（推奨度 95/100）** を正式採用します。
目的は、既存機能を壊さずに `/tales-claire` を追加し、
**「1ページ = 1メインコンポーネント」+「バレルインポート」+「共有部品の明確分離」** を実現することです。

---

## 0. 採用アーキテクチャ（Option D）

### ディレクトリ方針

```text
src/components/
├─ landing-page/
│  ├─ index.ts                       # export * from "./tales-claire"
│  └─ tales-claire/
│     ├─ index.ts                    # tales-claireの公開面
│     ├─ tales-claire-lp.tsx         # 1ページのメインコンポーネント
│     ├─ sections/                   # ページ固有セクション
│     └─ components/                 # ページ固有部品
├─ identity-visualization/           # 共有コンポーネント（独立）
├─ trial-entry/                      # 共有候補（再利用する場合）
└─ ui/                               # 既存共通UI（維持）
```

### ルーティング方針
- 既存 `/` は保持
- 新規 `/tales-claire` を追加
- `src/config/routes.ts` を定義元として更新し、`src/proxy.ts` は参照のみ

---

## 1. Phase 1: 環境準備（コア機能非破壊）

### 1.1 依存関係の整理と追加
- [x] 現在依存を確認（重複追加防止）
- [x] 追加候補のうち必要なもののみ導入
- [x] `tw-animate-css` は既存利用を継続（再追加しない）

対象候補:
- `@digital-go-jp/design-tokens`
- `@digital-go-jp/tailwind-theme-plugin`
- `nuqs`（実際にURL状態管理を使う場合のみ）
- `@hookform/resolvers`（フォーム検証で必要な場合のみ）

### 1.2 設定ファイル調整
- [x] `next.config.ts` に `images.remotePatterns` で `placehold.co` を追加
- [x] ローカル画像クエリ利用時は `images.localPatterns` も確認
- [x] `tsconfig.json` の `@/*` エイリアス利用前提を確認
- [x] `.gitignore` に必要な除外のみ追加（不要なコピー禁止）

### 1.3 新規ルート追加
- [x] `src/app/(public)/(static)/tales-claire/page.tsx` を作成
- [x] `src/config/routes.ts` に `ROUTES.TALES_CLAIRE = "/tales-claire"` を追加
- [x] `src/config/routes.ts` の `STATIC_PATHS` に `ROUTES.TALES_CLAIRE` を追加
- [x] `src/proxy.ts` は `STATIC_PATHS` 参照を維持（定義を持たせない）

### 1.4 バレル入口作成（必須）
- [x] `src/components/landing-page/index.ts` を作成
- [x] `src/components/landing-page/tales-claire/index.ts` を作成
- [x] `page.tsx` は `@/components/landing-page` から import する

### 1.5 Phase 1 検証
- [x] `pnpm install`
- [x] `pnpm dev`
- [x] `/tales-claire` プレースホルダー表示
- [x] `/`, `/home`, `/login` の既存動作確認

---

## 2. Phase 2: コンポーネント段階移行（Option D準拠）

### 2.1 共通UI差分の移植
- [x] `anti-supabase/src/components/ui` との差分を棚卸し
- [x] 既存 `src/components/ui/index.ts` の公開面を維持して不足分のみ追加
- [x] 既存と命名衝突する部品はリネームまたは統合方針を先に決定

### 2.2 グローバルスタイル統合
- [x] `glass` 系ユーティリティを `src/app/globals.css` に統合
- [x] 追加アニメーションを namespaced 命名で追加（衝突回避）
- [x] 既存テーマ（ライト/ダーク）との干渉を確認

### 2.3 tales-claire 固有部品の移行
- [x] `sections/*` にセクション群を配置
- [x] `components/*` に背景Canvas等の固有部品を配置
- [x] `tales-claire-lp.tsx` で全セクションを統合
- [x] 全セクション（hero, site-mission, purpose, inspiration, concept, declarations, final-goal, footer）移植完了
- [x] `BackgroundCanvas` コンポーネント移植完了

対象（例）:
- `hero-section.tsx`
- `site-mission-section.tsx`
- `purpose-section.tsx`
- `inspiration-section.tsx`
- `concept-section.tsx`
- `declarations-section.tsx`
- `final-goal-section.tsx`
- `footer.tsx`
- `background-canvas.tsx`

### 2.4 共有部品の移行
- [x] `identity-visualization` は独立ディレクトリとして移行
- [x] Container/Presentation分離を維持
- [x] `trial-entry` は再利用有無で配置先を決定

配置ルール:
- 再利用あり: `src/components/trial-entry/`
- tales-claire専用: `src/components/landing-page/tales-claire/components/`

### 2.5 メイン統合
- [x] `tales-claire-lp.tsx` をページ唯一のメインコンポーネントにする
- [x] `SidebarProvider` など依存Providerを入口で解決
- [x] `src/app/(public)/(static)/tales-claire/page.tsx` は薄いラッパーに限定

### 2.6 Phase 2 検証
- [x] `/tales-claire` 全セクション表示
- [x] レスポンシブ（375 / 768 / 1920）
- [x] ダークモード切替
- [x] アニメーション、ホバー、クリック動作

---

## 3. Phase 3: テスト・最適化・文書化

### 3.1 アセット移行
- [x] プレースホルダー画像設定（placehold.co）完了
- [ ] 本番画像への差し替え（後続タスク）

### 3.2 テスト実装（コピーではなく再整備）
- [x] コンポーネント近接テストを配置（コロケーション）
- [x] ページ統合テストを追加
- [x] 主要a11yテストを追加

### 3.3 ESLint/型整備
- [x] 新規ファイル範囲で厳格ルールを適用
- [x] `pnpm lint`, `pnpm test`, `pnpm build` を通す

### 3.4 パフォーマンス最適化
- [x] Canvas/ページ負荷を計測（Lighthouse: Performance 59, Accessibility 100）
- [x] 必要に応じて分割読み込み（`next/dynamic`）
- [x] メモ化と再レンダリング抑制を適用

### 3.5 ドキュメント
- [x] `docs/tales-claire-migration.md` を作成
- [x] 既存LPとの役割分担と運用ルールを明記

### 3.6 最終検証
- [x] 回帰: 認証、ホーム、管理画面
- [ ] Lighthouse: Performance 90+ / Accessibility 95+（現状: 59 / 100）
- [ ] 必要ならE2Eを追加

---

## 4. 実装時の必須ルール

- `src/proxy.ts` へルート定義を書かない（`src/config/routes.ts` 集約）
- 1ページは1メインコンポーネント（`tales-claire-lp.tsx`）
- import はバレル経由を優先（公開面を固定）
- `console.log` は使用しない（`logger.*` を使用）
- 既存ページ `/` は移行完了まで維持

---

## 5. 完了条件（Definition of Done）

- [x] `/tales-claire` が本番相当で動作
- [x] 既存ルートに回帰なし
- [x] バレル構造が固定され、import規約が守られている
- [x] テスト、lint、build が全て通過
- [x] 移行ドキュメントが更新済み
