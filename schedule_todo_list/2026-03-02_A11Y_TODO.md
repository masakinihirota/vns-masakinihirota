# 2026-03-02 アクセシビリティ（A11y）チェック TODO

> 方針: すべてのユーザー（視覚障害者・聴覚障害者・高齢者など）が等しくサービスを利用できる設計
> 対象: WCAG 2.1 AA準拠 / Vitest自動テスト / コントラスト・文字サイズ / セマンティクス / ARIA状態管理

---

## 🎯 ゴール

1. **WCAG 2.1 AA レベル**準拠（国際標準アクセシビリティガイドライン）
2. スクリーンリーダーで完全に操作・理解可能（特にトグルボタン等の状態変化）
3. キーボードのみで全機能を使用可能
4. 色・コントラスト・テキストサイズ（1rem基準）が視認性を満たす
5. アクセシビリティ監査を `vitest-axe` を用いて自動・定期実施

---

## ✅ Priority 1: アクセシビリティ基盤構築（4-5時間）

### 1. shadcn/ui コンポーネント導入（アクセシビリティ対応済み）
- [x] 1.1 すでに shadcn/ui が導入済みか確認
  - ✅ shadcn/ui は Radix UI ベースで ARIA 属性・キーボード操作に対応
- [ ] 1.2 使用中のコンポーネント全リスト作成
  - [ ] Button, Input, Select, Dialog, Tabs, ...
- [ ] 1.3 コンポーネント選定ガイド作成
  - [ ] エラーを表示するなら `Alert` コンポーネント
  - [ ] フォーム検証なら `Form` + `Input`
  - [ ] モーダルダイアログなら `Dialog` （focus trap付き）

**完了条件**
- [ ] 全コンポーネントが `aria-*` 属性 を正確に設定
- [ ] `vitest-axe` での自動チェック通過

### 2. HTML セマンティクスの確保
- [ ] 2.1 見出し（`<h1>～<h6>`）の階層正確化
  - [ ] 1ページ 1 つの `<h1>`
  - [ ] `<h2>` 飛ばして `<h3>` にしない
  - [ ] スクリーンリーダーで全見出しをナビゲート可能
- [ ] 2.2 ランドマーク（`<nav>`, `<main>`, `<aside>`, `<footer>`）の使用（ヘッダーは `<header>` に内包）
- [ ] 2.3 リスト構造（`<ul>`, `<ol>`, `<li>`）の活用
- [ ] 2.4 テーブル構造（`<table>`, `<thead>`, `<tbody>`, `<th>`）の正確性
- [ ] 2.5 ラベル（`<label>`）とフォーム コントロール（`<input>`, `<button>`）の紐付け

**完了条件**
- [ ] `<div>` でボタンを作るのをやめる（`<button>` 使用）
- [ ] `onClick` ハンドラが `<button>` のみ、`<div>` に付けない

### 3. キーボード操作の完全化
- [ ] 3.1 Tab キーで全インタラクティブ要素にアクセス可能
- [ ] 3.2 Enter キーでボタン・フォーム送信
- [ ] 3.3 Escape キーでモーダル・メニュー閉じる（言語切替Dropdown等も含む）
- [ ] 3.4 矢印キーで選択肢切り替え（Select, Tabs）
- [ ] 3.5 フォーカスインジケータが明確（`:focus-visible`）

**完了条件**
- [ ] マウスなしでサイト全体を通じたキーボードナビゲーションが可能

### 4. ARIA 属性による確実な状態通知（ヘッダーボタン周り等）
- [ ] 4.1 `role` 属性（正しい役割を明示）
- [ ] 4.2 **トグルボタンの状態管理**: 広告ON/OFFボタン等には必ず `aria-pressed="true" / "false"` を動的に反映し、スクリーンリーダーに現在の状態を伝える
- [ ] 4.3 **アイコンボタンのラベル**: ダークモード切替や言語切替など、テキストを持たないボタンには必ず `aria-label` を付与
  - 例: `<button aria-label="テーマを切り替える"><MoonIcon /></button>`
- [ ] 4.4 `aria-hidden="true"` で純粋な装飾要素を除外
- [ ] 4.5 `aria-live="polite"` / `aria-live="assertive"` でリアルタイム更新通知
- [ ] 4.6 メニュー開閉状態 (`aria-expanded`) の正確な提供

**完了条件**
- [ ] 各種アイコン・切り替えボタンで ARIA 属性が過剰・不足なく正確に機能する
- [ ] Wave（アクセシビリティ監査ツール）等でエラーゼロ

---

## 🟠 Priority 2: 視覚的アクセシビリティ（3-4時間）

### 5. 色・コントラスト
- [ ] 5.1 WCAG AA コントラスト比 4.5:1 以上（通常テキスト）
- [ ] 5.2 色だけで情報を伝えない（アイコン併用等）
- [ ] 5.3 tailwind.config.ts でカラートークン検証し、追加されたダークモード時の各色のコントラスト違反がないか確認

**完了条件**
- [ ] WebAIM Contrast Checker で ライトテーマ/ダークテーマともにAA 合格

### 6. テキストサイズ・読みやすさ・タイポグラフィ基準への準拠
- [ ] 6.1 デフォルトテキストサイズは `16px (1rem)` を基準とする（`14px` 以下は注釈等の例外を除き禁止）
- [ ] 6.2 単位は必ず `rem` を使用する
- [ ] 6.3 ユーザーがテキスト 200% 拡大可能（固定幅ビューポート回避）
- [ ] 6.4 行の高さ 1.5 以上（`leading-relaxed`）
- [ ] 6.5 高齢者への配慮: 各種トグル、ログインボタン、および新規追加の**お試しボタン**のクリックエリアは最低 **44x44px** 以上確保留保する

**完了条件**
- [ ] テキスト・ボタンサイズがプロジェクトルールを完全に満たす

### 7. アニメーション / 動き
- [ ] 7.1 `prefers-reduced-motion` 尊重（ダークモード切替時のトランジションなどを配慮）

**完了条件**
- [ ] `prefers-reduced-motion` でアニメーション OFF

---

## 🟡 Priority 3: フォーム・エラーハンドリング（2-3時間）

### 8. フォーム アクセシビリティ・メッセージング
- [ ] 8.1 すべての required フィールドに `aria-required="true"` または `required` 属性
- [ ] 8.2 エラー メッセージが `aria-describedby` で紐付け
- [ ] 8.3 エラーメッセージは専門用語やプレースホルダーを避け、平易な日本語で理由と解決策を提示する
- [ ] 8.4 入力支援（オートコンプリート）

### 9. リンク・ボタン・キャプション
- [ ] 9.1 リンク テキストが文脈で意味を持つ
- [ ] 9.2 ボタン テキストが動作を明示

---

## 🔵 Priority 4: 自動テスト・監視（3-4時間）

### 10. `vitest-axe` による自動テスト基盤構築
- [ ] 10.1 `vitest-axe` 導入
- [ ] 10.2 テスト例の作成
  ```typescript
  import { render } from '@testing-library/react';
  import { axe } from 'vitest-axe';
  import { ComponentName } from './component-name';

  describe('ComponentName Accessibility', () => {
    it('should have no accessibility violations', async () => {
      const { container } = render(<ComponentName />);
      const results = await axe(container);
      expect(results).toHaveNoViolations();
    });
  });
  ```
- [ ] 10.3 全UIコンポーネント（特に新設される Header とその中の各種トグルボタン）に `toHaveNoViolations` のチェックテストを追加

**完了条件**
- [ ] 全 UI コンポーネントが `vitest-axe` テストを通過

### 11. Playwright統合・手動テスト
- [ ] 11.1 Playwright E2Eスキャン
- [ ] 11.2 スクリーンリーダー（NVDA等）での手動テストフロー構築（ヘッダーで「広告ON」「広告OFF」と読み上げられるか確認など）

---

## 📊 進捗
- 完了: 0 / 53
- ステータス: 🔄 未開始
- 最後の更新: 2026-03-02 レビューによる基準厳格化、ヘッダー関連（ARIA状態管理・クリックエリア44px・ダークモード視認性）の要件を追加
